# Pinned for Python 3.12 (the Docker image). NOTE: numpy is intentionally <2 because
# insightface 0.7.3 is not fully numpy-2 compatible.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
python-multipart==0.0.20
psycopg[binary]==3.2.3
numpy==1.26.4
pillow==11.0.0
opencv-python-headless==4.10.0.84
onnxruntime==1.20.1
insightface==0.7.3
# For GPU inference: replace onnxruntime with onnxruntime-gpu and set USE_GPU=true
# (requires a CUDA-capable host + NVIDIA Container Toolkit).
#
# Auth deps (PyJWT, bcrypt) are installed in a separate Dockerfile layer on purpose, so
# adding them doesn't bust the expensive InsightFace build cache.
